class Member {
  constructor(id, ownerId, name, company, status, notes) {
    this.id = id;
    this.ownerId = ownerId;
    this.name = name;
    this.company = company;
    this.status = status;
    this.notes = notes
  }
}

export default Member;